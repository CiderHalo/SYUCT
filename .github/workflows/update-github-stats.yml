name: Update GitHub repository stats

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: GitHub stats are now browser-side
        run: |
          echo "Star/Fork display is fetched by assets/github-live-stats.js."
          echo "This workflow no longer writes assets/github-stats.json to the protected main branch."
