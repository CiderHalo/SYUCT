# SYUCT 校园指南 v1.10 更新说明

## 修复内容

- 修复手机端顶部导航栏空白。
- 修复桌面端继续显示旧导航栏的问题。
- 使用 `app-v110.js` 与 `styles-v110.css` 新文件名，彻底绕开浏览器和微信内置浏览器的旧缓存。
- 在每个页面写入静态导航栏兜底；即使 JavaScript 临时加载失败，也会显示校徽、标题和快捷入口。
- 标题统一为“沈化校园指南”。
- 左侧使用沈阳化工大学校徽。
- 学校官网和 GitHub 合并为一个快捷链接按钮，下拉后选择去向。
- 将主题初始化、导航栏、搜索等模块隔离执行，单个功能异常不会再导致整个顶部栏消失。

## 上传

解压增量包，将全部内容上传到仓库根目录并覆盖同名文件。

本次会新增：

- `assets/app-v110.js`
- `assets/styles-v110.css`

原来的 `assets/app.js` 和 `assets/styles.css` 可以保留，不会再被主要页面引用。
